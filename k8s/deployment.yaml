apiVersion: apps/v1
kind: Deployment
metadata:
  name: pjeda0j8-4418-staging
  namespace: qb-prototypes
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pjeda0j8-4418-staging
  template:
    metadata:
      labels:
        app: pjeda0j8-4418-staging
    spec:
      imagePullSecrets:
      - name: qtn-dockerhub
      containers:
      - name: web
        image: quantumteknologi/pjeda0j8-4418-staging
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"
          limits:
            cpu: "2"
            memory: "2Gi"
        env:
        - name: WATCHPACK_POLLING
          value: "true"
